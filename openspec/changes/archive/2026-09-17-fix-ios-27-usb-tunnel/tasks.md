## 1. Implementation

- [x] 1.1 以 TDD 實作「相容的裝置支援環境」，將 user runtime pin 與 capability selection切換到 host policy：macOS 27+ native、macOS 13–26 classic；delivery: iPhoneLocationMove/Device/Resources/pymobiledevice3.lock, iPhoneLocationMove/Device/RuntimeManager.swift, iPhoneLocationMoveTests/RuntimeManagerTests.swift；verification: `xcodebuild test -project iPhoneLocationMove.xcodeproj -scheme iPhoneLocationMove -destination 'platform=macOS' -only-testing:iPhoneLocationMoveTests/RuntimeManagerTests CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO`；regression: iPhoneLocationMoveTests/DeviceSetupStoreTests.swift, iPhoneLocationMoveTests/PymobiledeviceAdapterTests.swift；success: primary target輸出 `** TEST SUCCEEDED **`，且 macOS 27 native probe、缺少 `--native` 的拒絕案例與 macOS 13–26 classic probe案例通過；red: production edit前，host policy assertions因所有 host仍只收到 `lockdown start-tunnel`／`9.36.3` 而失敗
- [x] 1.2 [after: 1.1] 以 TDD 實作「macOS native tunnel 相容路徑」的 sealed offline runtime生成契約，從唯一 lock requirement重建 wheelhouse與 trust anchor；delivery: iPhoneLocationMoveTunnelHelper/Scripts/generate_tunnel_trust_anchor.py, iPhoneLocationMoveTunnelHelper/Scripts/tests/test_generate_tunnel_trust_anchor.py, iPhoneLocationMoveTunnelHelper/GeneratedTunnelTrustAnchor.swift, iPhoneLocationMoveTunnelHelper/Resources/tunnel-wheelhouse/；verification: `python3 -m unittest discover -s iPhoneLocationMoveTunnelHelper/Scripts/tests -p 'test_generate_tunnel_trust_anchor.py'`；regression: iPhoneLocationMoveTests/TunnelHelperContractTests.swift 的 generated manifest／digest assertions；success: primary target exit code為 0，且 lockfile缺失、多 requirement、primary wheel缺失、非 wheel payload及版本不一致案例皆 fail closed；red: production edit前，lockfile-derived generation assertions因 generator仍有獨立 `9.36.3` 常數或未拒絕無效 payload而失敗
- [x] 1.3 [after: 1.2] 以 TDD 完成「macOS native tunnel 相容路徑」的 helper launch契約，使用版本化 destination與 host policy，並在 native分支以 verified caller UID啟動固定 command；delivery: iPhoneLocationMoveTunnelHelper/main.swift, iPhoneLocationMoveTunnelHelper/Scripts/generate_tunnel_trust_anchor.py, iPhoneLocationMoveTunnelHelper/Scripts/tests/test_generate_tunnel_trust_anchor.py, iPhoneLocationMoveTunnelHelper/GeneratedTunnelTrustAnchor.swift, iPhoneLocationMoveTests/TunnelHelperContractTests.swift；verification: `xcodebuild test -project iPhoneLocationMove.xcodeproj -scheme iPhoneLocationMove -destination 'platform=macOS' -only-testing:iPhoneLocationMoveTests/TunnelHelperContractTests CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO` 與 `python3 -m unittest discover -s iPhoneLocationMoveTunnelHelper/Scripts/tests -p 'test_generate_tunnel_trust_anchor.py'`；regression: iPhoneLocationMoveTests/PymobiledeviceAdapterTests.swift, iPhoneLocationMoveTests/DisconnectReconnectIntegrationTests.swift；success: primary target輸出 `** TEST SUCCEEDED **`，且 macOS 27 fixed native arguments／verified UID environment、macOS 13–26 fixed classic arguments／無 native UID、versioned runtime與既有 ownership案例全部通過；generated trust anchor的 requirement直接引用 `TunnelRuntimePin.requirement`；另以帶 sentinel 的舊 `current` sibling證明 launcher只執行 pinned-version executable，且舊目錄內容與 metadata完全不變；red: production edit前，native policy、target UID、versioned destination、generated pin reference或 legacy sibling preservation assertions至少一項失敗並指出仍使用舊單一路徑 contract
- [x] 1.4 [after: 1.3] 以 TDD 擴充固定 production acceptance surface，加入走實際 preparation path的 `device-session-ready` case，並讓 runner、manifest、timeout fixture與文件一致使用版本化 runtime；delivery: iPhoneLocationMove/App/iPhoneLocationMoveApp.swift, iPhoneLocationMoveTests/AppLifecycleTests.swift, iPhoneLocationMoveTunnelHelper/Acceptance/cases.json, iPhoneLocationMoveTunnelHelper/Acceptance/prepare-endpoint-timeout.py, iPhoneLocationMoveTunnelHelper/Acceptance/run-production-acceptance.sh, iPhoneLocationMoveTunnelHelper/Acceptance/PRODUCTION_ACCEPTANCE.md, README.md；verification: `xcodebuild test -project iPhoneLocationMove.xcodeproj -scheme iPhoneLocationMove -destination 'platform=macOS' -only-testing:iPhoneLocationMoveTests/AppLifecycleTests CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO`；regression: `bash -n iPhoneLocationMoveTunnelHelper/Acceptance/run-production-acceptance.sh` 與 repository residual scan不得在 acceptance assets把 `/TunnelRuntime/current` 當作 active runtime；success: primary target輸出 `** TEST SUCCEEDED **`，固定 case parser包含 `device-session-ready`，negative cases只接受 manifest指定 typed error，且所有 acceptance assets指向 `pymobiledevice3-11.13.0`；red: production edit前，`device-session-ready` 不存在且 timeout fixture仍指向 `TunnelRuntime/current`
- [x] 1.5 [after: 1.4] 保存 macOS 27 + iOS 27 signed App／real XPC／real helper實機正向操作與自動回歸結果；delivery: openspec/changes/fix-ios-27-usb-tunnel/acceptance-results.md；verification: 使用者在 built signed App手動確認 USB iPhone可連線、操作並成功定位；regression: `xcodebuild test -project iPhoneLocationMove.xcodeproj -scheme iPhoneLocationMove -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO`, generator unittest, offline wheelhouse install, acceptance runner syntax與 fail-closed smoke checks；success: 使用者明確回報實機可操作和定位，且 acceptance-results.md記錄 272/272 macOS tests、8/8 generator tests、offline `pymobiledevice3==11.13.0`安裝成功及未執行的 full negative／cleanup suite；red: 舊版 baseline實機在 tunnel ready前失敗，無法進入可操作與定位狀態
