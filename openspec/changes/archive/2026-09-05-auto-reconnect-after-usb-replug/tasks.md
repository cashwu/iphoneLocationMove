## 1. adapter：tunnel exited 視為 USB 中斷

- [x] 1.1 [P] 為 adapter 的 tunnel `exited` 分類與 reconnect 收斂加入可失敗測試：把既有 diagnostics 案例的預期值從 `tunnelFailure("Tunnel process exited before recovery")` 改為 `usbDisconnected`，並新增案例驗證 exited 後 adapter state 為 `interrupted(reason usbDisconnected, positionKnowledge unknown)`、後續 `clearLocation` 拋 `usbDisconnected` 而非 `staleGeneration`、boundary 事件不含對該 lease 的 `stopTunnel`、`reconnect()` 以更大的 generation 依 reconcile、startTunnel、startDVT、clear 順序完成並進 ready、以新 generation 的 `setLocation` 成功、序列化 log 含恰好一次 `transport.recovery_failed` 與 `usb.disconnected` 事件且 `source=tunnel_exited` 而不含 raw detail、endpoint 與座標；ready 狀態直接呼叫 `reconnect()` 拋 `usbDisconnected` 且 state 仍為 `ready(session)`；`reconnect()` 內 clear 以非 `usbDisconnected` failure 失敗後 boundary 事件含對新 lease 的 `stopTunnel` 與 shutdownDVT、state 為 `cleanupPending`、後續 `clearLocation` 拋 `usbDisconnected`、第二次 `reconnect()` 成功且過程中同時存在的 lease 不超過一個；`reconnect()` 內 clear 的 recovery 探到 `exited` 時 state 為 `interrupted` 且 boundary 事件不含對該 lease 的 `stopTunnel`；delivery: iPhoneLocationMoveTests/PymobiledeviceAdapterTests.swift；verification: iPhoneLocationMoveTests/PymobiledeviceAdapterTests.swift 的 PymobiledeviceAdapterTests exited 與 reconnect 相關案例；regression: N/A，此步驟只新增或改寫尚未有對應實作的測試，primary target 即為該測試本身的完整相關範圍；success: exited 與 reconnect 相關案例全部執行並斷言 `usbDisconnected`、interrupted state、clear 回 `usbDisconnected`、reconnect 順序與新 generation set 成功、log 事件次數與敏感值排除、無中斷記錄時 reconnect 拒絕、clear 失敗後的 candidate 拆除與第二次 reconnect 成功；red: 既有 exited 案例仍收到 `tunnelFailure`、clear 案例收到 `staleGeneration`、reconnect 案例因無 disconnected device 記錄而收到 `usbDisconnected`、clear 失敗案例的第二次 reconnect 收到 `staleGeneration` 或 boundary 同時存在兩個 lease
- [x] 1.2 實作 Requirement「recovery terminal state 依 mutation 類型分流」的 exited 分支與「USB 中斷與安全重連」的 clear 失敗收斂：在 PymobiledeviceAdapter 抽出 private `markUSBDisconnected(oldSession:source:)`（遞增 recovery ownership epoch 與 logical generation、清除 session／lease／DVT handle／runtime、記錄 disconnected device、state 進 `interrupted(positionUnknown)`、遞增 transport generation、記錄帶 `source` 的 `usb.disconnected`），`handleUSBDisconnect` 先 capture generation 與 lease、改用它後再以 captured 值做既有的非同步清理；`recoverTransport` 的 status probe 在 `exited` 時呼叫它、不對該 lease 呼叫 `stopTunnel`、不自行記錄 `transport.recovery_failed`、直接拋 `usbDisconnected`，catch 對 `usbDisconnected` 原樣重拋不套用 `staleGeneration` 映射且 `recoveryMetadata` 的 `logicalGeneration` 改用 captured ownership 值；`reconnect()` 先 capture 新 lease，clear 失敗為 `usbDisconnected` 時直接重拋不拆除不覆寫 state，其他 failure 時先同步清除 session／lease／DVT handle／runtime、遞增 transport generation、保留 `disconnectedDeviceID`、state 設為 `cleanupPending`，再以 `try?` shutdownDVT 與對 captured lease stopTunnel，最後重拋；delivery: iPhoneLocationMove/Device/PymobiledeviceAdapter.swift；verification: iPhoneLocationMoveTests/PymobiledeviceAdapterTests.swift 的 PymobiledeviceAdapterTests exited 與 reconnect 相關案例；regression: iPhoneLocationMoveTests/PymobiledeviceAdapterTests.swift 既有的 recovery、disconnect、reconnect、switch 與 quit 案例，以及 iPhoneLocationMoveTests/DisconnectReconnectIntegrationTests.swift 既有的三個案例；success: exited 與 reconnect 相關案例全部通過；red: N/A，remaining-task：紅燈已於 1.1 建立，本任務是使其轉綠的實作步驟

## 2. SimulationStore：使用者動作的自動重備

- [x] 2.1 [P] 落實 Requirement「USB 中斷與安全重連」的 client seam 與測試 seam：`DeviceLocationClient` 新增 `reconnect() async throws -> PreparedDeviceSession`，不提供 protocol extension 預設實作；design Decision 2 列出的六個 fake（FakeSimulationDevice、ContentViewSimulationDevice、ResetTestSimulationDevice、ContentViewDevice、AppShellDevice、FakeSetupDevice）補上 `reconnect()` 預設拋 `usbDisconnected`；FakeSimulationDevice 增加 reconnect 結果佇列、`.suspended` 行為、`completeNextSuspendedReconnect(result:)` 與 `recordedReconnectCallCount()`；ResetTestSimulationDevice 增加 `failNextSet(_:)`、`suspendNextReconnect()`、`resumeReconnect(with:)` 與 `failReconnect(_:)`；AppShellDevice 增加 `failNextSet(_:)` 與 `enqueueReconnectResult(_:)`；DisconnectReconnectIntegrationTests 的 RecoveryBoundary 增加 `setTunnelStatus(_:)` 與 `failNextSet(_:)`；delivery: iPhoneLocationMove/Device/DeviceLocationClient.swift、iPhoneLocationMoveTests/SimulationStoreTests.swift、iPhoneLocationMoveTests/ContentViewTests.swift、iPhoneLocationMoveTests/AppShellTests.swift、iPhoneLocationMoveTests/DeviceSetupStoreTests.swift、iPhoneLocationMoveTests/DisconnectReconnectIntegrationTests.swift；verification: iPhoneLocationMove.xcodeproj 的 iPhoneLocationMove scheme 完整 macOS 測試套件；regression: N/A，primary target 即為完整測試套件，已涵蓋所有受 protocol 變更影響的 fake 與呼叫端；success: 完整測試套件編譯成功，且 iPhoneLocationMoveTests/PymobiledeviceAdapterTests.swift 以外的既有案例零失敗（該檔紅燈屬 1.1）；red: N/A，pure-refactor：只新增 protocol 需求與尚未被使用的測試 seam，不改變任何既有執行期行為
- [x] 2.2 為 SimulationStore 的自動重備加入可失敗測試：以 suspended reconnect 與 `$state` 歷史驗證單點與路線 start 在第一筆 set 拋 `usbDisconnected` 後 state 序列為 `starting`、`reconnecting(mode:)`、`starting`、`pointActive` 或 route running，`reconnect()` 恰好一次，第二筆 set 的 context generation 等於 reconnect 回傳的新 generation，`SimulationSessionID` 不變；reconnect 拋 `noUSBDevice` 或 `deviceNotFound` 時進入 `interrupted(positionUnknown)` 且 failure 與 reason 為 `usbDisconnected`、拋 `deviceLocked` 時 failure 為 `deviceLocked`，三者 set 只被呼叫一次；舊 route 因 USB 中斷 interrupted 後 `confirmPoint` 的 state 歷史含 `replacing`、`starting`、`reconnecting`、`starting`、`pointActive`，`reconnect()` 恰好一次且最終為新 `SimulationSessionID`；重發後再次 `usbDisconnected` 時進入 `interrupted` 且 reconnect 仍為一次；stop 的 clear 拋 `usbDisconnected` 時 state 維持 `stopping(failure: nil)`、reconnect 成功後回 `idle`、`activeSessionID` 為 nil、clear 呼叫次數為一；stop 的 reconnect 失敗時維持 `stopping(failure:)` 且再次 stop 會再次 clear 與 reconnect；reconnect suspended 期間再呼叫 `stop()` 或 `confirmPoint` 不產生第二次 reconnect 也不改變 state；`stopForQuit()` 在 reconnect suspended 時等待其完成後才 clear，等待期間再 enqueue 一個 `confirmPoint` 亦會被等到；route running 時 producer 的 set 拋 `usbDisconnected` 進入 interrupted 且 reconnect 次數為零；`reconnecting` 時 `confirmedRouteMarkerCoordinate` 為 nil；reconnect 拋帶 endpoint 字串的 `tunnelFailure` 時序列化 log 不含該字串；另在 DisconnectReconnectIntegrationTests 以真實 PymobiledeviceAdapter 加擴充後的 RecoveryBoundary 與 SimulationStore 驗證 tunnel `exited` 後確認單點會自動 reconnect 並進入 `pointActive` 且 `SimulationStore.generation` 等於 adapter 新 session 的 generation，以及 reconnect 內 clear 失敗後再次 stop 會第二次 reconnect 成功回 `idle`；並在 AppShellTests 以 AppShellDevice 的 `failNextSet` 與 `enqueueReconnectResult` 觸發並完成 auto reconnect，再以 ControllableMacLocationProvider 驗證 auto reconnect 完成後 DeviceSetupStore.state 仍為原 `ready(session)` 且 Mac 位置 request 次數不變；delivery: iPhoneLocationMoveTests/SimulationStoreTests.swift、iPhoneLocationMoveTests/DisconnectReconnectIntegrationTests.swift、iPhoneLocationMoveTests/AppShellTests.swift；verification: iPhoneLocationMoveTests/SimulationStoreTests.swift 的 SimulationStoreTests 自動重備案例；regression: iPhoneLocationMoveTests/DisconnectReconnectIntegrationTests.swift 既有的三個案例；success: 自動重備案例全部執行並斷言 state 序列、reconnect 次數、新 generation、failure 分類、replacement 起點、single-flight、quit 等待、最終狀態、marker 為 nil、setup state 不變與 log 排除；red: `SimulationStoreState.reconnecting` 尚不存在導致編譯失敗，或 start 在 `usbDisconnected` 後直接進入 `interrupted` 而 reconnect 次數為零
- [x] 2.3 為側欄的 `reconnecting` 呈現加入可失敗 view 測試：在同一 hosting view 中以 ResetTestSimulationDevice 的 `failNextSet` 與 `suspendNextReconnect` 讓 SimulationStore 停在 `reconnecting`，斷言側欄狀態區顯示「正在重新準備裝置…」、開始與 Reset 控制項 disabled、不存在「停止模擬」按鈕；`resumeReconnect` 後斷言進入 point active 或路線進度且不存在 `sidebar-simulation-error-region`；另一案例 `failReconnect(.noUSBDevice)` 後斷言顯示「模擬已中斷」與「USB 已中斷」；並把 `reconnecting` 加入既有的 busy 狀態矩陣與狀態切換 relayout 測試；delivery: iPhoneLocationMoveTests/ContentViewTests.swift；verification: iPhoneLocationMoveTests/ContentViewTests.swift 的 testResetIsDisabledForEveryBusySimulationState 與新增的 reconnecting 呈現案例；regression: iPhoneLocationMoveTests/ContentViewTests.swift 的 testConnectedSidebarRelayoutsObservedSimulationStates；success: reconnecting 呈現案例斷言到「正在重新準備裝置…」文字、disabled 控制項、無停止按鈕、resume 後無錯誤區、失敗後「模擬已中斷」與「USB 已中斷」文字，busy 矩陣含 reconnecting 並通過；red: `SimulationStoreState.reconnecting` 尚不存在導致編譯失敗，或側欄在 reconnecting 時找不到「正在重新準備裝置…」文字
- [x] 2.4 實作 Requirement「未偵測 USB 中斷後的自動重新準備」「停止模擬與 clear 確認」「模擬模式互斥與安全取代」「單點定位模式」「工作區重置」的自動重備與 busy 枚舉：SimulationStore 的 `generation` 改為 `private(set) var` 並只在 `reconnect()` 成功且動作仍 current 時更新；新增 `SimulationStoreState.reconnecting(mode:sessionID:)`；以 `userActionTask` 與 private `runUserAction` 取代 `commandInProgress`，`confirmPoint`、`startRoute`、`stop` 三個入口共用，`userActionTask` 由 outer task 於完成前清空；把 `SimulationLifecycleControlling` 的 SimulationStore extension 自 AppLifecycleCoordinator.swift 搬到 SimulationStore.swift，`stopForQuit()` 迴圈等待 `userActionTask`，`hasActiveSimulation` 含 `userActionTask != nil`；start 路徑以 state 仍為 `reconnecting` 判定 current；`confirmPoint` 與 `startRoute` 共用 private `performInitialSet`，對 `usbDisconnected` 最多 reconnect 一次並以新 generation 重發同一座標，reconnect 的 `noUSBDevice`／`deviceNotFound` 映射為 `usbDisconnected`，其他失敗走既有 interrupted 路徑；`stop` 對 `usbDisconnected` reconnect 一次、成功視同 clear 成功回 idle、回寫前確認 `activeSessionID == sessionID`、失敗維持 `stopping(failure:)`；producer 路徑不呼叫 `reconnect()`；`confirmedRouteMarkerCoordinate` 對 reconnecting 回 nil；記錄 `simulation.reconnect_started`、`simulation.reconnect_succeeded`、`simulation.reconnect_failed` 且 `failure` 只記 case 名稱，reconnect 失敗來源的 `*.start_failed`／`stop_failed` 亦只記 case 名稱；LocationMapView 的 simulation status 對 reconnecting 顯示「正在重新準備裝置…」、`simulationIsBusy` 回 true、`simulationHasCleanupOwnership` 回 false；delivery: iPhoneLocationMove/Features/Simulation/SimulationStore.swift、iPhoneLocationMove/Features/Map/LocationMapView.swift、iPhoneLocationMove/App/AppLifecycleCoordinator.swift；verification: iPhoneLocationMoveTests/SimulationStoreTests.swift 的 SimulationStoreTests 自動重備案例；regression: iPhoneLocationMoveTests/SimulationStoreTests.swift 既有案例、iPhoneLocationMoveTests/ContentViewTests.swift、iPhoneLocationMoveTests/DisconnectReconnectIntegrationTests.swift、iPhoneLocationMoveTests/AppShellTests.swift、iPhoneLocationMoveTests/AppLifecycleTests.swift；success: 自動重備案例全部通過；red: N/A，remaining-task：紅燈已於 2.2 與 2.3 建立，本任務是使其轉綠的實作步驟

## 3. 驗證

- [x] 3.1 以專案文件記載的指令執行完整 macOS 測試套件，確認無回歸且不需修改 project.yml 或 target membership；delivery: iPhoneLocationMove.xcodeproj；verification: iPhoneLocationMove.xcodeproj 的 iPhoneLocationMove scheme 完整 macOS 測試套件；regression: N/A，primary target 即為完整測試套件，已涵蓋本 change 所有受影響的測試檔；success: macOS 測試套件回報零失敗；red: N/A，remaining-task：本任務為最終驗證，其紅燈由前述各實作任務的個別測試涵蓋
- [x] 3.2 以實體 iPhone 手動確認三條使用者路徑：連接並 ready 後在閒置時拔除再插回，按「設定位置」或「開始路線」應先顯示「正在重新準備裝置…」再直接進入模擬且無紅字；閒置時拔除但不插回就按「設定位置」，應顯示「模擬已中斷」與「USB 已中斷」，插回後再按一次即成功；路線執行中拔除應顯示「模擬已中斷／USB 已中斷」，插回後按「停止模擬」應直接回到未啟用狀態；同時檢查 diagnostic log 含 `usb.disconnected source=tunnel_exited` 與 `simulation.reconnect_succeeded`；delivery: iPhoneLocationMove/Device/PymobiledeviceAdapter.swift、iPhoneLocationMove/Features/Simulation/SimulationStore.swift；verification: 以實體 iPhone 執行拔除再插回後開始模擬的手動確認；regression: 以同一台 iPhone 執行未插回即開始、以及路線中拔除後插回停止模擬的手動確認；success: 拔插後按一次開始即進入模擬且過程未出現「模擬已中斷」文字，log 含 `usb.disconnected` 與 `simulation.reconnect_succeeded`；red: 若仍出現「USB tunnel 失敗」或停止模擬卡在「清除定位失敗」則為未達成
