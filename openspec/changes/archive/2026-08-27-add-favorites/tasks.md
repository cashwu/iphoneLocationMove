## 1. TDD：先建立失敗測試

- [x] 1.1 [P] 新增 `iPhoneLocationMoveTests/FavoritesStoreTests.swift`（位於 test target 目錄掃描範圍，於 task 2.1 的 regen 一併納入編譯；紅燈階段可先以 regen 納入本檔），覆蓋 toggle 加入／取消與座標相等去重、預設名稱（有地址時為地址快照；address 為 nil 且座標 (25.033964, 121.564468) 時為「25.03396, 121.56447」的四捨五入 5 位小數格式）、remove 與不存在 id 的 no-op、rename 的 trim 與空字串 no-op、append 順序於變更與跨實例 roundtrip 後保持不變、載入時跳過緯度 91 等座標無效項目、載入時跳過單筆欄位毀損項目且其餘保留（lossy decode）、整份資料非合法清單結構時以空清單啟動、以寫入丟棄型 UserDefaults 子類驗證寫入不生效時記憶體狀態仍正確；delivery: iPhoneLocationMoveTests/FavoritesStoreTests.swift、iPhoneLocationMove.xcodeproj/project.pbxproj；verification: xcodebuild test -project iPhoneLocationMove.xcodeproj -scheme iPhoneLocationMove -destination 'platform=macOS' -only-testing:iPhoneLocationMoveTests/FavoritesStoreTests；regression: N/A（新測試檔即本 task 全部行為範圍，尚無既有最愛行為可回歸）；success: 該 suite 被編譯執行且每個新案例都以預期原因回報（而非無關錯誤）；red: FavoritesStore 型別尚未存在導致 test target build failure，或新 assertion 失敗
- [x] 1.2 [P] 在 `iPhoneLocationMoveTests/LocationMapModelTests.swift` 新增 selectFavorite regression tests：成功後 preview 含地址快照、previewCameraIntent 的 identity 等於推進後的新 generation、searchResults 清空且 activeSearchRequest 失效（舊 search 與 preview-address 回應判定 stale 不套用）、連續兩次選同一 place 產生兩個不同 intent identity；另依既有型別層慣例斷言 MapSearchGeneration(rawValue: .max).advanced() 丟出 identityExhausted；delivery: iPhoneLocationMoveTests/LocationMapModelTests.swift；verification: xcodebuild test -project iPhoneLocationMove.xcodeproj -scheme iPhoneLocationMove -destination 'platform=macOS' -only-testing:iPhoneLocationMoveTests/LocationMapModelTests；regression: 同 suite 既有 search／preview／reset 案例需維持通過；success: 新增案例以預期原因回報失敗或通過，且既有案例不因新增測試而失敗；red: selectFavorite 方法尚未存在導致 test target build failure
- [x] 1.3 [P] 在 `iPhoneLocationMoveTests/ContentViewTests.swift` 新增同一 rendered hierarchy 的 view regression tests：preview 面板收藏 toggle 按鈕在 FavoritesStore publisher 更新後於同一 hierarchy 切換加入／取消語義、點選最愛列後 preview 與 camera 依既有流程更新且先過 model ownership gate 才取消 view-local async、對某列提交 rename 後該列於同一 hierarchy 顯示新名稱且提交純空白後回顯原名、在 SimulationStore 已送出模擬目標的佈置下刪除當前 preview 對應收藏後 preview 與 annotation 維持不變且模擬狀態不變、工作區重置後最愛清單區塊仍顯示原有項目；delivery: iPhoneLocationMoveTests/ContentViewTests.swift；verification: xcodebuild test -project iPhoneLocationMove.xcodeproj -scheme iPhoneLocationMove -destination 'platform=macOS' -only-testing:iPhoneLocationMoveTests/ContentViewTests；regression: 同 suite 既有 sidebar／reset／搜尋選取案例需維持通過；success: 新增 view 案例以預期原因回報失敗或通過，且觀察對象是同一 rendered hierarchy 的輸出切換；red: FavoritesStore 或 view 收藏區塊尚未存在導致 build failure，或新 assertion 失敗

## 2. 實作

- [x] 2.1 [P] 新增 `iPhoneLocationMove/Features/Favorites/FavoritesStore.swift`（位於 app target 目錄掃描範圍），依 design.md Implementation Contract 實作 FavoritePlace、FavoritesStore（defaultsKey、注入 UserDefaults、逐元素 lossy decode 載入、toggle／remove／rename、append 順序即事實來源、每次變更後 JSON 寫回且寫入結果不影響記憶體狀態），並依 README 既有流程執行 xcodegen generate --spec iPhoneLocationMove/project.yml --project . --project-root . 重新產生 project.pbxproj 納入新增的 source 與 test 檔案；delivery: iPhoneLocationMove/Features/Favorites/FavoritesStore.swift、iPhoneLocationMove.xcodeproj/project.pbxproj；verification: xcodebuild test -project iPhoneLocationMove.xcodeproj -scheme iPhoneLocationMove -destination 'platform=macOS' -only-testing:iPhoneLocationMoveTests/FavoritesStoreTests；regression: N/A（primary suite 即為本 store 全部契約，其他 target 不受本 task 檔案影響）；success: FavoritesStoreTests 全數通過；red: N/A（red phase 已由 task 1.1 建立，本 task 屬 remaining-task 綠燈階段）
- [x] 2.2 [P] 在 `iPhoneLocationMove/Features/Map/LocationMapModel.swift` 新增 selectFavorite(_:)：推進 mapSearchGeneration、設定 preview 與以新 generation 為 identity 的 previewCameraIntent、清空 searchResults 與 activeSearchRequest、呼叫 recordUserMapContext()，唯一 throw 為 identityExhausted；delivery: iPhoneLocationMove/Features/Map/LocationMapModel.swift；verification: xcodebuild test -project iPhoneLocationMove.xcodeproj -scheme iPhoneLocationMove -destination 'platform=macOS' -only-testing:iPhoneLocationMoveTests/LocationMapModelTests；regression: 同 suite 既有 search／preview／camera／reset 案例；success: LocationMapModelTests 全數通過（含新 selectFavorite 案例）；red: N/A（red phase 已由 task 1.2 建立，本 task 屬 remaining-task 綠燈階段）
- [x] 2.3 加入收藏 UI 與單一實例注入鏈：`iPhoneLocationMove/App/iPhoneLocationMoveApp.swift` 的 AppDelegate 新增 let favoritesStore = FavoritesStore()；`iPhoneLocationMove/ContentView.swift` 的 ContentView 與 LocationWorkspaceView 傳遞該實例；`iPhoneLocationMove/Features/Map/LocationMapView.swift` 兩個 init 各加必要參數 favoritesStore 並以 @ObservedObject 持有、preview 面板 toggle 按鈕（標題依 isFavorite 切換，加 testingLayoutRegion sidebar-button-favorite-toggle）、「我的最愛」清單區塊（依 favorites 既有順序渲染、右鍵選單重新命名與刪除、行內 TextField 提交 rename 且編輯模式停用該列點選、Esc／失焦丟棄 pending 文字、加 testingLayoutRegion sidebar-favorites-list）、最愛列點選 action 先呼叫 model.selectFavorite 成功後才取消 view-local search／geocode task；delivery: iPhoneLocationMove/App/iPhoneLocationMoveApp.swift、iPhoneLocationMove/ContentView.swift、iPhoneLocationMove/Features/Map/LocationMapView.swift；verification: xcodebuild test -project iPhoneLocationMove.xcodeproj -scheme iPhoneLocationMove -destination 'platform=macOS' -only-testing:iPhoneLocationMoveTests/ContentViewTests；regression: 同 suite 既有 sidebar 布局、搜尋選取、reset 案例；success: ContentViewTests 全數通過（含新收藏 view 案例）；red: N/A（red phase 已由 task 1.3 建立，本 task 屬 remaining-task 綠燈階段）

## 3. 回歸與範圍檢查

- [x] 3.1 執行完整測試，確認 location simulation、mac map、route、reset 等既有行為無回歸；delivery: (none)（驗證性 task，不新增檔案）；verification: xcodebuild test -project iPhoneLocationMove.xcodeproj -scheme iPhoneLocationMove -destination 'platform=macOS'；regression: N/A（primary target 即完整 test suite，已涵蓋全部相關範圍）；success: 完整 suite 全數通過，無與本 change 相關的失敗；red: N/A（remaining-task：驗證性 task，無新失敗標記）
- [x] 3.2 檢查最終 diff 範圍：僅包含 proposal Impact 宣告的檔案、本 change artifacts 與 Cash review loop 產生的 openspec/signals 檔案；project.pbxproj 為 XcodeGen regen 產物，diff 只允許兩個新檔案的 registration 與 generator 既有的無關緊要正規化，不得夾帶未宣告的 resources、build defaults、scheme churn 或 xcuserstate 等個人 workspace 狀態，`iPhoneLocationMove/project.yml` 不得有 diff；delivery: (none)（驗證性 task，不新增檔案）；verification: 以 git status 與 git diff 逐檔人工檢視 diff 範圍的 manual assertion；regression: N/A（範圍檢查本身即完整涵蓋，無其他 regression target）；success: diff 檔案清單與 proposal Impact 一致且 pbxproj diff 僅含新檔 registration 與 regen 正規化；red: N/A（remaining-task：驗證性 task，無新失敗標記）
