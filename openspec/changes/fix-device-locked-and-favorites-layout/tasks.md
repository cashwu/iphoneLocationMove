## 1. 裝置失敗訊息

- [ ] 1.1 為失敗摘要規則加入可失敗測試：涵蓋鎖定裝置的實際 traceback 只留例外摘要、折行摘要接合為單行、無 traceback 時取最後一個有意義的錯誤行、僅含警告時退回離開碼訊息、standard output 不併入 standard error 例外、超過長度上限時截斷；delivery: iPhoneLocationMoveTests/PymobiledeviceAdapterTests.swift；verification: iPhoneLocationMoveTests/PymobiledeviceAdapterTests.swift 的 PymobiledeviceFailureSummaryTests；regression: N/A，此步驟只新增尚未有對應實作的測試，primary target 即為該測試本身的完整相關範圍；success: PymobiledeviceFailureSummaryTests 涵蓋鎖定 traceback 摘要、折行接合、無 traceback 的多行輸出取最後一個有意義的行、僅含警告退回離開碼、standard output 不併入 standard error 例外、standard error 無內容時改用 standard output、超過長度上限截斷並以省略記號結尾、方框未閉合時 frame 標頭與原始碼行號回顯不進入摘要、輸出在 traceback 標題後截斷時標題不成為摘要等案例並全部執行；red: 摘要型別尚不存在導致編譯失敗，或斷言比對到未過濾的 NotOpenSSLWarning、traceback 內容或被接上的 standard output 內容而失敗
- [ ] 1.2 實作失敗摘要規則：新增 PymobiledeviceFailureSummary，standard error 與 standard output 各自獨立摘要且不接合為同一句、standard output 只在 standard error 無可用內容時作為顯示來源，以最後一個方框邊界為界抽取例外摘要並以單一空白接合折行，過濾警告、traceback 標題、frame 標頭與原始碼行號回顯，無方框時取最後一個有意義的行，全為雜訊時退回離開碼訊息，並對顯示用細節套用 400 字元上限；delivery: iPhoneLocationMove/Device/PymobiledeviceAdapter.swift；verification: iPhoneLocationMoveTests/PymobiledeviceAdapterTests.swift 的 PymobiledeviceFailureSummaryTests；regression: iPhoneLocationMoveTests/PymobiledeviceAdapterTests.swift 既有的 PymobiledeviceAdapterTests；success: PymobiledeviceFailureSummaryTests 的摘要案例（含 standard output 隔離與長度上限截斷）全數通過；red: N/A，remaining-task：紅燈已於 1.1 建立，本任務是使其轉綠的實作步驟
- [ ] 1.3 為裝置鎖定分類加入可失敗測試：驗證鎖定 traceback 的摘要被判定為裝置鎖定、且不被判定為授權失敗，並驗證既有的 pairing 訊息仍被判定為授權失敗；delivery: iPhoneLocationMoveTests/PymobiledeviceAdapterTests.swift；verification: iPhoneLocationMoveTests/PymobiledeviceAdapterTests.swift 的 PymobiledeviceFailureSummaryTests；regression: N/A，此步驟只新增尚未有對應實作的分類測試，primary target 即為該測試本身的完整相關範圍；success: 鎖定分類與授權分類兩個案例皆執行並回報結果；red: 裝置鎖定判定函式尚不存在導致編譯失敗
- [ ] 1.4 實作裝置鎖定的 typed 失敗與呈現：DeviceLocationError 新增 deviceLocked、DeviceRecoveryAction 新增 unlockDevice，DeviceFailurePresentation 回傳可與既有標題區分的鎖定標題、指示解鎖並保持螢幕開啟的訊息與 unlockDevice 復原動作；delivery: iPhoneLocationMove/Device/DeviceLocationClient.swift、iPhoneLocationMoveTests/DeviceFailurePresentationTests.swift；verification: iPhoneLocationMoveTests/DeviceFailurePresentationTests.swift；regression: iPhoneLocationMoveTests/DeviceLocationClientTests.swift；success: DeviceFailurePresentationTests 的鎖定呈現案例與標題相異性案例通過；red: deviceLocked 尚未納入標題相異性清單時該案例失敗，或鎖定呈現案例因缺少對應分支而失敗
- [ ] 1.5 讓分類順序可被測試並接上：把「輸出加階段轉為 typed 失敗」抽為 PymobiledeviceFailureSummary 上的 classify pure function，依先鎖定、再授權、最後該階段 prerequisite 的順序回傳，分類在截斷前的細節上進行，且鎖定判定跨 standard error 與 standard output 兩個串流摘要一併進行後才輪到授權判定，使鎖定不被另一串流的授權標記吸收；LivePymobiledeviceBoundary 的私有方法改為直接丟出 classify 結果並移除舊的 failureDetail 與授權判定。分類順序原本住在 private actor 內而無法被測試建構，既有 PymobiledeviceAdapterTests 全走 FakePymobiledeviceBoundary 直接注入 typed error 而不經過此路徑，因此不存在可涵蓋此順序的既有測試；delivery: iPhoneLocationMove/Device/PymobiledeviceAdapter.swift、iPhoneLocationMoveTests/PymobiledeviceAdapterTests.swift；verification: iPhoneLocationMoveTests/PymobiledeviceAdapterTests.swift 的 PymobiledeviceFailureSummaryTests 中的 classify 案例；regression: iPhoneLocationMoveTests/PymobiledeviceAdapterTests.swift 既有的 PymobiledeviceAdapterTests；success: classify 在 developerDiskImage 階段的鎖定輸入回傳 deviceLocked、trust 階段的鎖定輸入回傳 deviceLocked、lockdown 原始鎖定錯誤字串回傳 deviceLocked、單行 pairing 訊息回傳 authorizationDenied、pairing 的方框式 traceback 回傳 authorizationDenied、未分類輸入回傳帶原階段與摘要的 prerequisiteFailed、鎖定標記落在 400 字元之後仍回傳 deviceLocked、鎖定標記只出現在 standard output 時仍回傳 deviceLocked、同一行同時含鎖定與授權標記時回傳 deviceLocked、鎖定標記在 standard output 而授權標記在 standard error 時回傳 deviceLocked、鎖定標記在 standard error 而授權標記在 standard output 時回傳 deviceLocked、未分類失敗的 message 超過上限時被截斷至上限並以省略記號結尾，十二個 classify 案例全部通過；red: classify 尚不存在導致編譯失敗，或把鎖定與授權兩個分支對調時同時含兩種標記的案例回傳 authorizationDenied。實務上 pymobiledevice3 的鎖定訊息不含授權關鍵字、兩組關鍵字互斥，故順序僅能以刻意構造的輸入觀察，該案例用途是釘住優先序契約以防日後關鍵字集合變動時靜默翻轉

## 2. 最愛儲存體與側欄版面

- [ ] 2.1 [P] 讓側欄 view 測試使用隔離的偏好設定來源：地圖 hosting view 建構輔助方法的最愛儲存體參數預設值改為一次性隔離 suite，另一處直接建構最愛儲存體的測試同樣改用隔離 suite；delivery: iPhoneLocationMoveTests/ContentViewTests.swift；verification: iPhoneLocationMoveTests/ContentViewTests.swift；regression: iPhoneLocationMoveTests/FavoritesStoreTests.swift；success: ContentViewTests 全數通過且結果不隨執行機器既有收藏而改變；red: 在已存有收藏的機器上，未隔離前 testConnectedSidebarRelayoutsObservedSimulationStates 因側欄找不到設定位置按鈕而失敗
- [ ] 2.2 [P] 移除最愛儲存體的偏好設定來源預設值並更新 App 建構點：FavoritesStore 的 initializer 移除 UserDefaults 預設值，App 的兩個建構點改為明示傳入正式偏好設定域；delivery: iPhoneLocationMove/Features/Favorites/FavoritesStore.swift、iPhoneLocationMove/ContentView.swift、iPhoneLocationMove/App/iPhoneLocationMoveApp.swift；verification: iPhoneLocationMoveTests/FavoritesStoreTests.swift；regression: iPhoneLocationMoveTests/ContentViewTests.swift；success: FavoritesStoreTests 全數通過且專案中不存在未指明來源的最愛儲存體建構；red: N/A，pure-refactor：移除預設值不改變任何既有呼叫端的執行期行為，僅使來源成為必填參數
- [ ] 2.3 為側欄版面加入可失敗回歸測試：驗證收藏數遠超過可見列數上限時設定位置按鈕仍位於側欄可視範圍內、最愛區塊高度與剛超過上限時相同、該情形下最愛區塊內存在其 documentView 高於 clip view 的捲動容器以證明所有收藏可捲動存取，而收藏數未達門檻時不存在該捲動容器，且收藏數未達上限時區塊高度小於剛超過上限時的高度以證明未預留額外空白；測試建立的隔離 UserDefaults suite 於結束時清除持久化內容；delivery: iPhoneLocationMoveTests/ContentViewTests.swift；verification: iPhoneLocationMoveTests/ContentViewTests.swift 的 testFavoritesListNeverPushesDeviceControlsOutOfTheSidebar；regression: N/A，此步驟只新增尚未有對應實作的版面測試，primary target 即為該測試本身的完整相關範圍；success: 該測試的五個斷言（可視性、高度封頂、門檻上存在捲動容器、門檻下不存在捲動容器、門檻下高度較小）全部執行並回報結果；red: 未調整版面前，設定位置按鈕不在可視範圍而使必要按鈕集合比對為空集合，且二十筆與六筆的最愛區塊高度不相等
- [ ] 2.4 調整側欄版面：把最愛區塊移到 iPhone 定位控制之後、工作區訊息之前並於區塊開頭加入分隔線，並在收藏數超過五筆時把列的堆疊包入固定高度 104 的 ScrollView、未超過時直接列出，列的堆疊抽出為共用的 view 屬性；delivery: iPhoneLocationMove/Features/Map/LocationMapView.swift；verification: iPhoneLocationMoveTests/ContentViewTests.swift 的 testFavoritesListNeverPushesDeviceControlsOutOfTheSidebar；regression: iPhoneLocationMoveTests/ContentViewTests.swift 既有的 testConnectedSidebarRelayoutsObservedSimulationStates 與 testFavoriteToggleUpdatesTheRenderedHierarchy；success: 版面回歸測試通過且既有側欄與最愛互動測試維持通過；red: N/A，remaining-task：紅燈已於 2.3 建立，本任務是使其轉綠的實作步驟

## 3. 驗證

- [ ] 3.1 以專案文件記載的指令執行完整 macOS 測試套件與 Python protocol 測試，確認無回歸；delivery: iPhoneLocationMove.xcodeproj、iPhoneLocationMoveHelper/tests；verification: iPhoneLocationMove.xcodeproj 的 iPhoneLocationMove scheme 完整 macOS 測試套件；regression: iPhoneLocationMoveHelper/tests 的 Python protocol 測試；success: macOS 測試套件回報零失敗且 Python protocol 測試回報 OK；red: N/A，remaining-task：本任務為最終驗證，其紅燈由前述各實作任務的個別測試涵蓋
- [ ] 3.2 以實體 iPhone 手動確認鎖定指引，涵蓋 DDI 與 trust 兩個階段：螢幕鎖定時連接裝置（含尚未信任的裝置以觸發 trust 階段鎖定）應顯示解鎖指引而非該階段原本的修復資訊，解鎖後重試應可完成準備；同時記錄實際觀察到的鎖定錯誤字串，確認已被鎖定關鍵字涵蓋；delivery: iPhoneLocationMove/Device/PymobiledeviceAdapter.swift、iPhoneLocationMove/Device/DeviceLocationClient.swift；verification: 以鎖定螢幕的實體 iPhone 執行 App 裝置準備流程的手動確認；regression: 以解鎖螢幕的同一台 iPhone 重試裝置準備流程；success: DDI 與 trust 兩個階段在鎖定時畫面都顯示解鎖指引，且不含 Python traceback 與 Xcode 版本建議；red: 若任一階段仍顯示該階段原本的修復資訊或整段 traceback 則為未達成
